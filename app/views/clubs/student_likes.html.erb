<div class="container">
  <h2><%= link_to @club.name,club_path(@club.id) %>をお気に入り登録してる生徒一覧</h2>
  <% @student_likes.each do |s|%>
  <div class="index-profile">
    <%= link_to "",student_path(s.id) %>
    <div class="index-avatar">
    <% if s.avatar.attached? %>
      <%= image_tag s.avatar.variant(gravity: :center, resize:"50x50^", crop:"50x50+0+0").processed %>
    <% else %>
      <div class="no-image">
      <p>No image</p>
      </div>
    <% end %>

    </div>
    <div class="index-rightbox">
      <div class="index-name">
        <%= s.name %>
      </div>
      <div class="index-detail">
        <%= s.detail%>
      </div>
    </div>
  </div>
  <% end %>
  <%= paginate @student_likes%>

</div>