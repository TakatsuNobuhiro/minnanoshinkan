<%= form_with(model: [@event, current_student.event_favorites.build], remote: true) do |f| %>
  <%= hidden_field_tag :student_id, current_student.id %>
  <%= f.submit 'いいねをする',class: 'btn btn-success' %>
<% end %>