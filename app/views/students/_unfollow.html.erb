<%= form_with(model: current_student.relationships.find_by(follow_id: @student.id), remote: true, method: :delete) do |f| %>
<%= hidden_field_tag :follow_id, @student.id %>
<%= f.submit 'フォロー解除',class: 'btn btn-danger' %>
<% end %>